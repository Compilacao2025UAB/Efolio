// Teste: mistura de formas de declaracao e inicializacao de vetor.
int main() {
    int a, b = read(), c = 2 * b, v[] = {1, 2.5, 3};
    write(a);      // Espera-se: 0
    write(b);      // Deve escrever o valor lido
    write(c);      // Deve escrever 2*b
    writev(v);     // Deve escrever {1,2,3}
    return 0;
} 